name: DeployDist
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "greet user"
        run: |
          echo "cloning repo..."
          echo "${{toJson(github)}}"
          git clone https://github.com/${{github.repository}}.git
